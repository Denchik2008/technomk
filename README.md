# E-Commerce Website - ТехноМиКс

Полнофункциональный интернет-магазин с React на фронтенде, Express.js на бэкенде и SQLite в качестве базы данных.

## 🎯 Возможности

### Основной сайт:
- 🏠 **Главная страница** - товары по акции, хиты продаж и новинки
- 📦 **Каталог товаров** - фильтрация по категориям и поиск
- 🔍 **Детальная страница товара** - полное описание, цена, отзывы
- ⭐ **Система отзывов** - оставить отзыв и просмотреть отзывы других
- 🛒 **Корзина покупок** - изменение количества товаров
- ✅ **Оформление заказа** - с контактными данными
- 📂 **Каталоги** - выпадающее меню с категориями в header
- 🔀 **Сортировка** - по цене (возрастание/убывание)
- ❤️ **Избранное** - сохраняется в localStorage
- 📱 **Адаптивный дизайн** - для всех устройств

### Панель администратора (`/admin`):
- ➕ **Управление товарами** - добавление, редактирование, удаление
- 📂 **Управление категориями** - создание и удаление категорий
- 📋 **Просмотр заказов** - невыполненные и выполненные
- ✔️ **Изменение статуса заказов** - пометка как выполненные
- 🏷️ **Плашки товаров** - hit и new

## 🛠 Технологии

**Frontend:**
- React 18
- React Router DOM 6
- CSS3 с Material Icons

**Backend:**
- Node.js
- Express.js
- Better-SQLite3

**База данных:**
- SQLite (4 таблицы: products, orders, order_items, reviews, categories)

## 🚀 Установка и запуск

### 1. Установка зависимостей

Установите зависимости для сервера:

\`\`\`bash
npm install
\`\`\`

Установите зависимости для клиента:

\`\`\`bash
cd client
npm install
cd ..
\`\`\`

### 2. Инициализация базы данных

Создайте базу данных и заполните её тестовыми данными:

\`\`\`bash
npm run init-db
\`\`\`

### 3. Запуск приложения

#### Рекомендуемый способ (Windows):

\`\`\`powershell
.\\start-dev.ps1
\`\`\`

Этот скрипт автоматически:
- Останавливает старые процессы Node
- Очищает переменную окружения PORT
- Запускает сервер и клиент одновременно

#### Альтернативный способ:

\`\`\`bash
npm run dev
\`\`\`

Или запустите их отдельно:

Сервер (порт 5000):
\`\`\`bash
npm run server
\`\`\`

Клиент (порт 3000):
\`\`\`bash
npm run client
\`\`\`

### 4. Открытие приложения

- **Основной сайт:** http://localhost:3000
- **Панель администратора:** http://localhost:3000/admin
- **API сервер:** http://localhost:5000

## 📁 Структура проекта

\`\`\`
.
├── server/
│   ├── index.js          # Express сервер с API endpoints
│   └── init-db.js        # Скрипт инициализации БД
├── client/
│   ├── public/
│   │   └── index.html    # HTML шаблон
│   └── src/
│       ├── components/   # React компоненты
│       │   ├── Header.js
│       │   ├── Footer.js
│       │   └── ProductCard.js
│       ├── pages/        # Страницы приложения
│       │   ├── Home.js
│       │   ├── Catalog.js
│       │   ├── ProductDetail.js
│       │   ├── Cart.js
│       │   └── Admin.js
│       ├── App.js        # Главный компонент
│       └── index.js      # Точка входа
├── shop.db               # База данных SQLite
├── start-dev.ps1         # Скрипт запуска (Windows)
├── package.json          # Зависимости сервера
└── README.md
\`\`\`

## 🔌 API Endpoints

### Товары
- `GET /api/products` - Получить все товары
- `GET /api/products/:id` - Получить товар по ID
- `POST /api/products` - Создать новый товар
- `PUT /api/products/:id` - Обновить товар
- `DELETE /api/products/:id` - Удалить товар

### Заказы
- `GET /api/orders` - Получить все заказы (опционально ?status=pending|completed)
- `POST /api/orders` - Создать новый заказ
- `PUT /api/orders/:id/status` - Обновить статус заказа

### Категории
- `GET /api/categories` - Получить все категории
- `POST /api/categories` - Создать новую категорию
- `DELETE /api/categories/:id` - Удалить категорию

### Отзывы
- `GET /api/products/:id/reviews` - Получить отзывы товара
- `POST /api/products/:id/reviews` - Добавить отзыв

## 💾 База данных

### Таблица products
- `id` - PRIMARY KEY
- `name` - Название товара
- `price` - Цена
- `category` - Категория
- `description` - Описание
- `image` - URL изображения
- `isHit` - Хит продаж (0/1)
- `isNew` - Новинка (0/1)
- `created_at` - Дата создания

### Таблица orders
- `id` - PRIMARY KEY
- `customer_name` - Имя клиента
- `customer_email` - Email клиента
- `customer_phone` - Телефон клиента
- `customer_address` - Адрес доставки
- `total` - Общая сумма заказа
- `status` - Статус (pending/completed)
- `created_at` - Дата создания

### Таблица order_items
- `id` - PRIMARY KEY
- `order_id` - ID заказа (FOREIGN KEY)
- `product_id` - ID товара (FOREIGN KEY)
- `quantity` - Количество
- `price` - Цена на момент заказа

### Таблица reviews
- `id` - PRIMARY KEY
- `product_id` - ID товара (FOREIGN KEY)
- `customer_name` - Имя автора
- `rating` - Оценка (1-5)
- `comment` - Комментарий
- `created_at` - Дата создания

### Таблица categories
- `id` - PRIMARY KEY
- `name` - Название категории (UNIQUE)
- `description` - Описание
- `created_at` - Дата создания

## ✨ Особенности

- **Корзина и избранное** сохраняются в localStorage браузера
- **Адаптивный дизайн** работает на всех устройствах
- **Material Icons** для единообразного UI
- **SQLite** - не требует отдельного сервера БД
- **REST API** для всех операций с данными
- **Система отзывов** с рейтингом 1-5 звезд
- **Выпадающее меню** каталогов в header
- **Сортировка товаров** по цене
- **Детальные страницы** товаров с полным описанием

## 🔧 Разработка

При разработке используйте режим `npm run dev` или `.\\start-dev.ps1`, который запускает оба сервера одновременно с hot reload.

## 📝 Примечания

- Для доступа к панели администратора перейдите по адресу http://localhost:3000/admin
- В текущей версии нет системы аутентификации. В production-среде рекомендуется добавить авторизацию
- Если у вас установлена системная переменная `PORT`, используйте скрипт `start-dev.ps1` для запуска

## 📄 Лицензия

MIT

